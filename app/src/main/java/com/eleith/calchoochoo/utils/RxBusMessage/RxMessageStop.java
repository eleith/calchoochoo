package com.eleith.calchoochoo.utils.RxBusMessage;

import com.eleith.calchoochoo.data.Stop;

public class RxMessageStop extends RxMessage<Stop>{
  public RxMessageStop(String key, Stop stop) {
    super(key, stop);
  }
}
